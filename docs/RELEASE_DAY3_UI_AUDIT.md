# RELEASE DAY 3 UI AUDIT

Дата: 2026-04-23

## Что исправлено (закрыто)

1. P0: Инвертированная тема на экране Premium
- Файл: `app/(premium)/index.tsx`
- Проблема: на части устройств цвета темы рендерились в обратной логике.
- Исправление: экран Premium использует `colors` из `ThemeProvider` напрямую.
- Статус: ✅ закрыто.

2. P0: «Квакозябры» на модалке добавления места
- Файл: `components/AddPlaceModal.tsx`
- Проблема: повреждённые UTF-8 строки (заголовки, placeholder, empty-state, символ рейтинга), а также некорректный `slugify`.
- Исправление: полный рефактор файла в корректной UTF-8 кодировке, восстановлены русские строки и генерация slug.
- Статус: ✅ закрыто.

3. P1: Duplicate key в списке маршрута
- Файлы: `app/trip/result.tsx`, `app/trip/[id].tsx`
- Проблема: ключи `key={seg.id}` и `key={place.id}` могли конфликтовать при данных с повторяющимися id.
- Исправление: устойчивые composite-keys с индексами (`${id}-${index}` и контекст дня).
- Статус: ✅ закрыто.

## Верификация

- `npx tsc --noEmit` — passed
- `npm run smoke:content` — passed
- `npm run smoke:api` — passed

## Чек-лист ручного UI-прогона (критичные экраны)

- Premium экран: открытие, переключение темы, CTA покупки/restore — ✅
- Result/Trip detail: табы, карточки дней, добавление места — ✅ (после фикса UTF-8 и ключей)
- Wallet: открытие раздела и карточек документов — ✅ базово
- Fallback-блоки transport/hotels с CTA — ✅

## Остаточные UI-риски (бэклог)

1. P2: Неполная theme-адаптация legacy-экранов
- Ряд экранов ещё использует статические `Colors`, а не `useTheme()`.

2. P2: Нужна единая типографика/копирайтинг fallback-сообщений
- Формулировки отличаются между `result/[id]`, `transport-select`, `hotel-select`.

3. P3: Визуальная унификация карточек partner/live-источников
- Разные размеры/иерархия текстов между блоками поездов/авиа/отелей.

## Вывод

Критичные UI-дефекты Дня 3 (P0/P1), влияющие на стабильность и читаемость интерфейса, устранены. Проект готов к следующему дню работ по полировке (P2/P3).
